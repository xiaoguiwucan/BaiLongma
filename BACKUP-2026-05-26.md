# BaiLongma 当前版本备份与 Mac 自部署说明

备份日期：2026-05-26
当前维护仓库：[xiaoguiwucan/BaiLongma](https://github.com/xiaoguiwucan/BaiLongma)
原始上游仓库：[xiaoyuanda666-ship-it/BaiLongma](https://github.com/xiaoyuanda666-ship-it/BaiLongma)
当前应用版本：`2.3.2`
推荐运行方式：Mac 本地源码运行 Electron 桌面版；需要安装包时执行 `npm run build:mac`

这份文档用于记录当前这版白龙马的代码状态、语音能力改动、Git/GitHub 备份方法，以及在另一台 Mac 上从零恢复部署的方法。

## 0. 版本记录要求

从 `v2.1.209` 开始，每次版本备份都必须同步更新：

- `CHANGELOG.md`
- `docs/VOICE_EVENTS_PROTOCOL.md`：写清版本号、日期、更新内容、改变原因、影响范围、部署注意事项。
- `README.md`：更新当前版本号，并在“版本更新记录”里补摘要。
- Brain UI 设置页：在“更新 -> 更新说明”里补最近版本摘要。
- `BACKUP-YYYY-MM-DD.md`：写清当前备份版本、Git tag、哪些文件上传、哪些文件留在本地。
- `package.json` 和 `package-lock.json`：版本号必须一致。
- GitHub Release：每个版本都必须发布 Release，Release 正文必须写清更新内容、改变原因、部署方法、附件说明和已知限制。
- Release 附件：需要上传该版本源码包和 Git bundle；如有安装包，也要一并上传。

备份不是只提交代码，也要让以后的人看得懂这个版本为什么存在、改了哪里、怎么恢复。




## v2.3.2 语音唤醒稳定性补丁 - 2026-05-26

本次 v2.3.2 收束 v2.3.1 之后的语音稳定性检查点，Git tag 为 `v2.3.2`。重点是视频/音乐播放时的唤醒可靠性、可诊断性，以及 partial ASR 不再绕过唤醒门控。

### 本次新增重点

- 视频/音乐 pre-roll 环形音频缓存：媒体播放时保留最近 0.8–4 秒麦克风音频，近场人声确认后先 flush 预录音再继续 ASR。
- 设置页新增“视频/音乐预录音缓存”和“预录音时长”。
- 后端 voice config 新增 `videoVoicePreRollEnabled` / `videoVoicePreRollMs`。
- 语音调试面板显示 VAD/麦克风、唤醒原因、声纹分数、媒体门控/pre-roll 状态。
- ASR partial 只用于显示和调试，不再触发自动发送；final 且通过唤醒/声纹门控后才是正式指令。

### v2.3.2 验证命令

```bash
node --check src/ui/brain-ui/voice-panel.js
node --check src/ui/brain-ui/app.js
node --check src/ui/brain-ui/app-shell.js
node --check src/config.js
node --check src/api.js
node --check scripts/smoke-brain-ui.mjs
node --check scripts/smoke-voice-panel-gating.mjs
npm run smoke:voice-panel-gating
npm run smoke:brain-ui
npm run smoke:voice-events
npm run smoke:voice-manager
npm run build:mac
```

### v2.3.2 Release 附件

发布时应上传：

- `BaiLongma-source-v2.3.2.zip`
- `BaiLongma-v2.3.2.bundle`
- `Bailongma-2.3.2-arm64.dmg` / `Bailongma-2.3.2-mac-arm64.zip`（如本机打包成功）

## v2.3.1 Mac Electron 打包部署补丁 - 2026-05-26

本次 v2.3.1 是 v2.3.0 之后的 Mac 桌面部署补丁版本，Git tag 为 `v2.3.1`。它不改变语音功能主逻辑，重点把当前项目从 Windows-first 打包配置调整为 Mac-first 桌面发布配置。

### 本次新增重点

- 默认构建命令：`npm run build` 改为 `npm run build:mac`。
- Mac 打包命令：新增 `npm run build:mac`，默认生成 Apple Silicon arm64 macOS `.dmg` / `.zip`；Intel Mac 可使用 `npm run build:mac:x64`。
- Mac 发布命令：新增 `npm run publish:mac`，并让 `npm run publish` 默认走 Mac 发布。
- Windows 兼容：原 Windows NSIS 构建保留为 `npm run build:win` / `npm run publish:win`。
- 跨平台清理：新增 `scripts/prebuild-clean.mjs`，Mac 不再依赖 PowerShell。
- macOS 图标：新增 `build/icon.icns`，由 `build/icon-mac-cartoon.png` 生成。
- 发布仓库：electron-builder GitHub publish 目标修正为 `xiaoguiwucan/BaiLongma`。

### v2.3.1 Mac 打包部署方法

```bash
cd BaiLongma
npm install
npm run build:mac
open dist
```

生成的 `.dmg` 可用于本机安装；`.zip` 可用于自动更新或备份分发。当前没有配置 Apple Developer ID 签名和 notarization，分发到其他 Mac 时可能需要在“系统设置 -> 隐私与安全性”允许打开。

### v2.3.1 验证命令

```bash
node --check scripts/prebuild-clean.mjs
node --check src/ui/brain-ui/app-shell.js
node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); JSON.parse(require('fs').readFileSync('package-lock.json','utf8'))"
npm run smoke:voice-manager
npm run smoke:voice-events
npm run smoke:brain-ui
npm run build:mac
```

### v2.3.1 Release 附件

发布时应上传：

- `BaiLongma-source-v2.3.1.zip`：Git 追踪源码快照。
- `BaiLongma-v2.3.1.bundle`：离线 Git bundle。
- `Bailongma-2.3.1-*.dmg` / `Bailongma-2.3.1-*.zip`：如本机 macOS 打包成功，则一并上传。

不上传：`.env`、`config.json`、`data/`、`node_modules/`、模型目录、虚拟环境和运行时缓存。

## v2.3.0 大版本备份补充 - 2026-05-26

本次 v2.3.0 是 v2.2.0 之后本地语音稳定性检查点的正式收束版本，Git tag 为 `v2.3.0`。

### 本次新增重点

- 本地语音一键准备：`/voice/local/readiness` 与 `/voice/local/readiness/apply`。
- 本地语音实测闭环：`/voice/local/self-test/start` 与 `/voice/local/self-test`。
- 本地语音总览：`/voice/local/overview`，聚合服务、readiness、声纹、自测、麦克风和下一步动作。
- 本地诊断包：`/voice/local/diagnostics/package`，不包含 API Key、原始音频或声纹内容。
- 服务复用：自动检测并复用已有 3723 本地 ASR 服务，避免重复启动。
- 声纹安全：防锁死、清除、离线清除、备份、选择备份恢复、阈值校准。
- 麦克风自检：实时当前/峰值/噪声底/阈值显示，一键按当前峰值校准触发阈值，并在本地语音总览中显示麦克风状态。

### v2.3.0 验证命令

```bash
node --check src/api.js
node --check src/config.js
node --check src/voice/manager.js
node --check src/ui/brain-ui/app-shell.js
node --check src/ui/brain-ui/app.js
node --check src/ui/brain-ui/voice-panel.js
node --check scripts/smoke-brain-ui.mjs
node --check scripts/smoke-voice-events.mjs
node --check scripts/smoke-voice-manager.mjs
python3 -m py_compile src/voice/sensevoice_server.py
npm run smoke:voice-manager
npm run smoke:voice-events
npm run smoke:brain-ui
```

### v2.3.0 Release 附件

发布时应上传：

- `BaiLongma-source-v2.3.0.tar.gz`：Git 追踪源码快照。
- `BaiLongma-v2.3.0.bundle`：离线 Git bundle。

不上传：`.env`、`config.json`、`data/`、`node_modules/`、模型目录、虚拟环境和打包产物。

## 1. 当前版本状态

本次备份对应的是在原项目基础上增强后的 Mac Electron 桌面助手版本，重点集中在本地语音链路、视频播放场景、设置页交互，v2.1.211 新增的小智式语音状态机底座，v2.1.212 增强的严格/宽松唤醒词流程，v2.1.213 增强的多样本声纹稳定性，v2.1.214 增强的视频播放抗干扰配置，v2.1.215 新增的 ASR Provider/Profile 基础，v2.1.216 新增的分句式 TTS Session，v2.1.217 新增的小智式语音事件协议，v2.1.218 新增的实验性语音 WebSocket 事件通道，v2.1.219 新增的 TTS 音频段就绪事件，v2.1.220 新增的可选 WebSocket TTS 音频块订阅，v2.1.221 新增的 WebSocket `tts:speak` 直接合成请求，v2.1.222 新增的 `tts:cancel` 与 speak 生命周期守卫，v2.1.223 新增的语音 WebSocket 调试客户端，v2.1.224 新增的 `/voice/events` 协议文档，v2.1.225 新增的语音协议 smoke 测试，v2.1.226 增强的 `/voice/events/publish` 映射 smoke 覆盖，v2.1.227 新增的 wake/tts 映射 smoke 覆盖，v2.1.228 新增的完整 TTS lifecycle 映射 smoke 覆盖，v2.1.229 新增的 voice mapping 纯函数 smoke，v2.1.230 新增的 `/voice/events/protocol` 协议元数据端点和共享协议常量，v2.1.231 新增的 `/voice/events` 客户端消息校验与 `protocol_error` 结构化错误回执，v2.1.232 新增的 `tts:speak` 最大文本长度和单连接冷却限制，v2.1.233 新增的限制可配置能力，v2.1.234 新增的 `/voice/events` 可选 token 鉴权和访问检查，v2.1.235 新增的 `tts:speak` 远端地址级冷却，v2.1.236 新增的客户端身份登记，v2.1.237 新增的客户端 capabilities 和 lastSeenAt 诊断，v2.1.238 增强的 voice events CLI 身份/能力调试能力，v2.1.239 新增的 audio_negotiation 音频能力协商，以及 v2.1.240 新增的 `/voice/events/clients` 客户端诊断端点。

主要能力：

- Electron 桌面版入口：`npm start`
- 后端服务：`http://127.0.0.1:3721`
- Brain UI：`http://127.0.0.1:3721/brain-ui`
- 本地 ASR WebSocket：`ws://127.0.0.1:3723`
- 默认本地语音识别：`SenseVoiceSmall`
- Whisper 保留为可切换备用模型
- 设置页支持选择语音识别服务商与本地模型
- 设置页支持配置唤醒词
- 设置页支持录入声纹，并开启“只响应我的声音”
- 设置页支持调节声纹严格度，当前默认值为 `0.55`
- 视频播放时增加语音抗干扰能力：
  - 检测到近场人声时自动降低/暂停视频
  - 视频播放时可启用空格按住说话
  - 可开启系统回声消除 AEC
- 本地 ASR 端增强了静音门控和低质量文本过滤，减少 Whisper/SenseVoice 在空音频、视频背景音下输出重复幻觉文本
- v2.1.211 新增 Voice State Machine，统一语音交互状态，并记录 roundId / asrSessionId / ttsSessionId
- v2.1.212 新增严格/宽松唤醒词模式、可配置唤醒窗口和重复误识别文本抑制
- v2.1.213 新增多样本声纹录入、声纹测试和校准分数反馈
- v2.1.214 新增视频降音目标、降音保持时间、人声触发灵敏度和降音状态显示
- v2.1.215 新增 ASR Provider 元数据和极速/平衡/高精度识别模式
- v2.1.216 新增分句式 TTS Session、中文分句器和前端播放队列
- v2.1.217 新增小智式 wake/asr/tts/interrupt/media 统一事件协议
- v2.1.218 新增 `/voice/events` WebSocket 事件通道和小智式 JSON 映射
- v2.1.219 新增 `tts:audio_ready`，WebSocket 客户端可获取分句音频 URL、序号、文本和 contentType
- v2.1.220 新增可选 WebSocket TTS 音频块订阅，客户端显式订阅后可接收 base64 或二进制 `audio/mpeg` chunk
- v2.1.221 新增 WebSocket `tts:speak`，外部客户端可直接发文本并接收分句事件与音频 chunk
- v2.1.222 新增 WebSocket `tts:cancel`，同连接新 speak 自动替换旧 speak，断线自动取消当前 speak
- v2.1.223 新增 `npm run voice:events` 调试客户端，支持 status/listen/speak/cancel 和音频保存
- v2.1.224 新增 `docs/VOICE_EVENTS_PROTOCOL.md`，固化小智式 `/voice/events` v3 协议说明
- v2.1.225 新增 `npm run smoke:voice-events`，自动验证 hello/ping/subscribe/cancel/status
- v2.1.226 增强 `smoke:voice-events`，覆盖 `/voice/events/publish` raw voice_event 和小智式 stt final 映射
- v2.1.227 继续增强 `smoke:voice-events`，覆盖 wake:accepted 和 tts:audio_ready 小智式映射
- v2.1.228 继续增强 `smoke:voice-events`，覆盖 tts:start/sentence_start/audio_ready/sentence_end/stop 完整生命周期映射
- v2.1.229 导出 `mapVoiceEventToXiaozhi` 并新增 `npm run smoke:voice-mapping` 13 项映射检查
- v2.1.230 新增 `/voice/events/protocol` 协议元数据端点和共享协议常量
- v2.1.231 新增 `/voice/events` 客户端消息校验与 `protocol_error` 结构化错误回执
- v2.1.232 新增 `tts:speak` 最大文本长度和单连接冷却限制
- v2.1.233 新增 `tts:speak` 限制设置项并同步到设置页、协议端点和 WebSocket hello
- v2.1.234 新增 `/voice/events` 可选 token 鉴权与远端访问检查
- v2.1.235 新增 `tts:speak` 远端地址级冷却，防止多连接绕过限流
- v2.1.236 新增 `client:hello` / `client:identify` 客户端身份登记和 status clientDetails
- v2.1.237 新增 client capabilities 与 lastSeenAt 诊断
- v2.1.238 增强 `npm run voice:events` 调试客户端，支持 protocol 命令、自动 client:hello、身份参数、capability 参数和 `--no-identify`；新增 `npm run smoke:voice-events-client`。
- v2.1.239 新增 `/voice/events` `audio_negotiation` 能力，`client:accepted.negotiated` 会根据 `binary_audio` / `base64_audio` 返回推荐音频模式。
- v2.1.240 新增 `GET /voice/events/clients`，聚焦展示外部客户端身份、订阅状态、capabilities 和 negotiated 诊断。
- 设置页新增小智式语音状态调试面板，用于排查唤醒、声纹、ASR、TTS 打断和后续分句 TTS 问题

## 2. 本次关键文件

这些文件应进入 Git 和 GitHub：

- `.gitignore`
- `src/api.js`
- `src/config.js`
- `src/voice/manager.js`
- `src/voice/asr-providers.js`
- `src/voice/sentence-splitter.js`
- `src/voice/tts-session.js`
- `src/voice/voice-events.js`
- `src/voice/voice-event-bus.js`
- `src/voice/voice-state-machine.js`
- `src/voice/sensevoice_server.py`
- `src/voice/whisper_server.py`
- `src/ui/brain-ui/app-shell.js`
- `src/ui/brain-ui/app.js`
- `src/ui/brain-ui/styles.css`
- `src/ui/brain-ui/voice-panel.js`
- `task_plan.md`
- `findings.md`
- `progress.md`
- `BACKUP-2026-05-26.md`

这些文件或目录不应进入 GitHub：

- `.env`：本地密钥
- `config.json`：本地运行配置，可能包含用户自己的服务商设置
- `data/`：记忆、对话、声纹等本地数据
- `.venv-whisper/`：Python 虚拟环境，可重建
- `models/SenseVoiceSmall/`：本地模型文件，体积较大，可重新下载
- `node_modules/`：Node 依赖，可通过 `npm install` 重建
- 打包产物：`dist/`、`dist-v2/`、`voice-dist/`

## 3. Git/GitHub 备份流程

在当前机器上备份：

```bash
cd "/Users/imac/开发项目/codx/白龙马agent"

git status --short --branch
git add .gitignore src/api.js src/config.js src/voice/manager.js src/voice/sensevoice_server.py src/voice/whisper_server.py src/ui/brain-ui/app-shell.js src/ui/brain-ui/app.js src/ui/brain-ui/styles.css src/ui/brain-ui/voice-panel.js task_plan.md findings.md progress.md BACKUP-2026-05-26.md
git commit -m "backup: mac electron local voice assistant"
git tag backup-2026-05-26-local-voice
git push origin main
git push origin backup-2026-05-26-local-voice
```

查看备份是否成功：

```bash
git log --oneline -5
git ls-remote --tags origin | grep backup-2026-05-26-local-voice
```

## 4. 源码包备份方法

如果只需要打一个干净的源码包，不包含密钥、模型、虚拟环境和数据库，可以执行：

```bash
cd "/Users/imac/开发项目/codx/白龙马agent"
mkdir -p backups
git archive --format=tar.gz --output="backups/BaiLongma-source-2.1.209-20260526.tar.gz" HEAD
```

这个包只包含 Git 已追踪文件。恢复时解压后仍需重新安装 Node/Python 依赖和下载模型。

如果需要备份本地个人数据，可单独备份以下目录和文件，不建议上传 GitHub：

```bash
cd "/Users/imac/开发项目/codx/白龙马agent"
mkdir -p backups/private-20260526
cp -R data backups/private-20260526/data
cp config.json backups/private-20260526/config.json
cp .env backups/private-20260526/.env
```

注意：`data/voiceprint.json` 是你的声纹数据，属于个人敏感数据。它应该只保存在本机或你信任的私有备份中。

## 5. Mac 从零部署

### 5.1 系统准备

建议环境：

- macOS Apple Silicon 或 Intel Mac
- Node.js 20 LTS 或更高
- Python 3.11
- Git
- Xcode Command Line Tools

安装 Xcode Command Line Tools：

```bash
xcode-select --install
```

确认版本：

```bash
node -v
npm -v
python3.11 --version
git --version
```

如果没有 Python 3.11，可用 Homebrew 安装：

```bash
brew install python@3.11
```

### 5.2 拉取项目

```bash
cd ~/开发项目
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
```

如果要恢复本次备份点：

```bash
git fetch --tags
git checkout backup-2026-05-26-local-voice
```

如果希望继续在最新主分支开发：

```bash
git checkout main
git pull origin main
```

### 5.3 安装 Node 依赖

```bash
npm install
```

如果 `better-sqlite3` 或 Electron 原生依赖报错，可尝试：

```bash
npm rebuild
npx electron-rebuild -f -w better-sqlite3
```

### 5.4 配置 LLM API Key

创建 `.env`：

```bash
touch .env
```

最小示例：

```env
LLM_PROVIDER=minimax
MINIMAX_API_KEY=你的_KEY
```

也可以不手写 `.env`，首次打开后在激活页配置：

```text
http://127.0.0.1:3721/activation
```

### 5.5 创建本地语音 Python 环境

```bash
cd "/你的路径/BaiLongma"
python3.11 -m venv .venv-whisper
source .venv-whisper/bin/activate
python -m pip install --upgrade pip setuptools wheel
```

安装 Whisper、SenseVoice 和声纹依赖：

```bash
pip install openai-whisper websockets numpy soundfile
pip install funasr modelscope huggingface_hub
pip install resemblyzer webrtcvad-wheels
```

如果 Apple Silicon 上安装 PyTorch 有问题，可单独安装：

```bash
pip install torch torchaudio
```

### 5.6 下载 SenseVoiceSmall 模型

推荐把模型放在项目目录：

```bash
mkdir -p models
```

方式一：使用 Hugging Face：

```bash
source .venv-whisper/bin/activate
huggingface-cli download FunAudioLLM/SenseVoiceSmall --local-dir models/SenseVoiceSmall
```

方式二：使用 ModelScope：

```bash
source .venv-whisper/bin/activate
modelscope download --model iic/SenseVoiceSmall --local_dir models/SenseVoiceSmall
```

下载完成后确认至少存在模型文件：

```bash
ls models/SenseVoiceSmall
```

也可以不预下载。首次启动 SenseVoice 时，程序会尝试从远端模型仓库加载，但预下载更稳定。

### 5.7 启动桌面版

```bash
npm start
```

启动后打开：

```text
http://127.0.0.1:3721/brain-ui
```

语音本地服务默认端口：

```text
ws://127.0.0.1:3723
```

### 5.8 推荐语音设置

在 Brain UI 设置页进入“语音识别”：

- 服务商：`本地`
- 本地模型：`SenseVoiceSmall`
- 识别语言：`中文`
- 启用唤醒词：开启
- 默认唤醒词：`小龙马，龙马，白龙马`
- 只响应我的声音：声纹录入成功后再开启
- 声纹严格度：先用 `0.55`
- 检测到人声时自动降低/暂停视频：开启
- 视频播放时启用空格按住说话：开启
- 启用系统回声消除 AEC：开启

声纹录入建议：

- 录 5 到 10 秒
- 离麦克风保持日常使用距离
- 用正常说话音量
- 说几句自然中文，不要只念一个词
- 先在安静环境录入
- 如果视频场景识别不稳定，后续可重新录入一版带轻微背景声的样本

## 6. 启动、关闭和重启

启动 Electron：

```bash
npm start
```

只启动后端：

```bash
npm run start:backend
```

开发模式：

```bash
npm run dev
```

关闭当前服务：

```bash
pkill -f "electron ."
pkill -f "src/index.js"
pkill -f "sensevoice_server.py"
pkill -f "whisper_server.py"
```

查看端口占用：

```bash
lsof -i :3721
lsof -i :3723
```

## 7. 常见问题

### 7.1 语音服务启动失败

先确认 Python 环境存在：

```bash
ls .venv-whisper/bin/python
```

再手动启动 SenseVoice 服务看日志：

```bash
source .venv-whisper/bin/activate
python src/voice/sensevoice_server.py --model sensevoice-small --port 3723
```

### 7.2 SenseVoice 模型找不到

确认：

```bash
ls models/SenseVoiceSmall
```

如模型在别的位置，可设置环境变量：

```bash
export BAILONGMA_SENSEVOICE_MODEL_DIR="/你的模型路径/SenseVoiceSmall"
npm start
```

### 7.3 声纹录入后识别不到

先把声纹严格度降到 `0.55` 或 `0.50` 测试。
如果仍不稳定，删除旧声纹后重新录：

```bash
rm -f data/voiceprint.json
```

然后在设置页重新录入。

声纹不是唤醒词模型，它需要足够清晰的人声。视频声音太大时，建议同时开启视频降音、按住说话和 AEC。

### 7.4 播放视频时唤醒失败

优先检查：

- 视频音量是否过大
- Mac 麦克风输入音量是否过低
- 是否开启“检测到人声时自动降低/暂停视频”
- 是否开启“视频播放时启用空格按住说话”
- 是否开启 AEC

在特别吵的场景，推荐按住空格说话，这是最稳定的人机交互方式。

### 7.5 出现“我只想说了”等重复幻觉文本

当前版本已经在 Whisper 和 SenseVoice 服务里加入了过滤。若仍出现，优先：

- 提高触发阈值
- 开启唤醒词
- 开启声纹
- 降低视频音量
- 使用 SenseVoiceSmall 而不是 Whisper

## 8. 当前版本限制

- SenseVoiceSmall 是本地 ASR，不是本地大模型助手。
- 唤醒词目前仍是软件侧文本/音频链路判断，不等同于 ESP32 WakeNet 这类专用 KWS。
- 声纹目前使用 `resemblyzer`，适合个人桌面辅助，但还不是工业级 3D-Speaker/ECAPA 声纹系统。
- 视频抗干扰依赖浏览器/系统可用的 AEC、音量控制和前端检测逻辑，极端噪声下仍建议使用按住说话。
- macOS 打包配置已在 v2.3.1 作为主路径补齐；当前可源码运行，也可执行 `npm run build:mac` 生成 dmg/zip。

## 9. 下一步建议

推荐下一版继续做：

- 增加 2 到 3 秒 pre-roll 环形音频缓存（v2.3.2 已完成视频/音乐场景版本）
- 将唤醒词检测与正式 ASR 文本彻底分离（v2.3.2 已阻止 partial ASR 自动发送）
- 接入真正本地 KWS，例如 sherpa-onnx KWS 或 openWakeWord
- 声纹升级为 3D-Speaker 或 ECAPA，并支持多样本注册
- 增加语音调试面板，显示 VAD、声纹分数、唤醒命中、拒绝原因（v2.3.2 已增强）
- 后续如需正式对外分发，补 Apple Developer ID 签名和 notarization


## v2.2.0 大版本备份补充 - 2026-05-26

本次 v2.2.0 是按“制作大版本更新，不要改动一点点就更新”的要求集中发布的小智式语音设备控制台版本。

### 主要新增

- Brain UI 外部语音客户端控制台：连接数、音频订阅、二进制订阅、身份、capabilities、lastSeenAt、negotiated 音频模式、健康建议。
- `/voice/events/onboarding`：统一生成本机/LAN 接入地址、调试命令、`client:hello` 和 subscribe 示例。
- `/voice/events/history`：最近语音事件历史，支持 `limit` 与 `type`，同时返回 raw event 与 Xiaozhi-style mapping。
- `/voice/events/summary`：语音链路总控摘要，聚合客户端、订阅、wake/asr/tts/interrupt 计数和中文排障建议。
- `/voice/events/check`：一键自检协议、客户端、握手、订阅、binary audio、事件历史和“唤醒→ASR→TTS”闭环。
- `/voice/events/package`：生成设备接入包，包含 README、`.env.voice`、`client-hello.json`、`subscribe.json`、Node 示例和 ESP32 伪配置。

### Mac 本地部署

```bash
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
npm install
npm start
```

如需局域网设备接入，建议设置：

```bash
export BAILONGMA_ALLOW_LAN=true
export BAILONGMA_API_TOKEN="换成强随机token"
npm start
```

设备侧先访问：

```bash
curl http://127.0.0.1:3721/voice/events/package
```

然后按接入包中的 `README.md`、`client-hello.json`、`subscribe.json` 和 WebSocket 示例实现设备端。

### 验证命令

```bash
node --check src/voice/voice-event-bus.js
node --check src/api.js
node --check src/ui/brain-ui/app.js
npm run smoke:voice-mapping
npm run smoke:voice-events
npm run smoke:voice-events-client
npm run smoke:brain-ui
```

### 备份资产

发布 v2.2.0 时需要上传：

- `BaiLongma-v2.2.0-source.tar.gz`：源码快照，排除 `.git`、`node_modules`、临时 dist 产物。
- `BaiLongma-v2.2.0.bundle`：Git bundle，可离线恢复完整仓库历史。

恢复方式：

```bash
git clone BaiLongma-v2.2.0.bundle BaiLongma-v2.2.0
cd BaiLongma-v2.2.0
npm install
npm start
```
