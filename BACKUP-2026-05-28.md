## v0.4.53 - 2026-05-29

### 版本情况
- 版本标签：`v0.4.53`。
- 本版本是“微信群图片理解链路修复”版本，解决连续 @、补充文字、再发图片时只拿到 `[图片]` 占位的问题。
- 已在本机重启贾维斯，当前运行进程已加载新代码，Wechaty 已自动恢复登录并接入目标群。

### 修复内容
- @ 消息如果是纯 @ 或图片理解请求，会等待短窗口，合并同一成员后续文字/图片后再判断。
- 图片已入库时优先走直接识图回复；识图失败会回报模型错误，而不是让文本模型猜测。
- 图片入库改为后台识图，避免消息入口被坏模型卡住。
- 识图候选去重并调整优先级，避免同一个模型/中转被当前配置和 LLM Profile 重复调用。

### 部署方法
```bash
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
git checkout v0.4.53
npm install
./start-jarvis-background.sh
```

### 验证方法
1. 保持 Wechaty 显示在线。
2. 在已勾选的群里依次发送：`@前夜`、`总结一下图`、一张图片。
3. 预期：程序先等待约 3 秒；如果模型可用，会回复图片总结；如果模型不可用，会明确提示图片已入库但识图模型失败。

### 不进入 GitHub 的数据
- 微信登录态、群聊原文数据库、图片 base64/媒体文件、API Key、日志仍保存在本机 `~/Library/Application Support/Bailongma`，不提交到仓库。

## v0.4.52 - 2026-05-29

### 版本情况
- 版本标签：`v0.4.52`。
- 本版本是“Web 微信系统级 @ 可行性实验”版本，不改变当前生产默认回复策略。
- 新增接口只用于本机调试：`POST /social/wechaty-duty-group/test-native-mention`。

### 实验记录
- 已在当前运行的贾维斯中重启并加载新代码。
- 已连接 Wechaty / wechat4u，状态为 `connected / online`。
- 在“值班群”对成员“风”发送 4 条测试消息：
  - `Msg.MsgSource`
  - `Msg.MsgSource + Msg.msgsource`
  - `Msg.msgsource`
  - 顶层 `MsgSource + Msg.MsgSource`
- 4 条消息均成功发送，但本机 Mac 微信会话列表没有出现「有人@我」，只显示普通未读和文本预览。

### 结论和后续
- Web 微信服务端目前会接受消息 JSON，但不会把我们注入的 `MsgSource/atuserlist` 变成系统级 @。
- 当前 `wechaty-puppet-wechat4u` 只能稳定做到可见文本 `@昵称`。
- 如果用户必须要第二张图那种系统级提示，下一版应设计“Mac 微信 UI 自动化发送模式”：用官方 Mac 微信客户端执行真实 @ 选择和发送。

### 恢复/部署
```bash
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
git checkout v0.4.52
npm install
./start-jarvis-background.sh
```


## v0.4.51 - 2026-05-29

### 备份说明
- 本版本是 v0.4.50 的 @ 可靠性热修复版，重点修复微信群回复和 LLM 渠道告警 @ 不显示微信昵称的问题。
- 版本标签：`v0.4.51`。部署后无需重新配置群组；已有 sender_id 配置会继续使用，并在发送时解析为真实群昵称。

### 修复内容
- wechat4u 链路不再依赖被忽略的 mentionIdList，而是手动拼出 `@微信群昵称`，确保手机端能看到明确 @ 对象。
- 发送前清理模型自行生成的开头 @，统一由底层重建真实提问人的 @。
- 群安全拒绝回复补齐 mentionName，避免安全拦截消息 @ 不到提问人。

### 验证
- `node --check src/social/wechaty-duty-group.js src/llm-connectivity-monitor.js src/ui/brain-ui/app.js`
- `npm run test:wechat-guard`

## v0.4.50 - 2026-05-29

### 备份说明
- 本版本是在 v0.4.49 LLM 渠道连通通知基础上的通知对象增强版。代码、配置结构、页面说明和 Release 文档均同步更新。
- 版本标签：`v0.4.50`。部署后刷新 Brain UI，进入 `设置 -> LLM 模型 -> 渠道连通通知`，在每个通知微信群卡片里点击“加载成员”即可选择要 @ 的微信成员。

### 新增 / 改动
- LLM 渠道连通通知新增按群选择 @ 人员，配置保存为 `llmConnectivityMonitor.notifyMentionsByGroup`。
- 通知发送时支持多人 @；普通微信群助手单人 @ 回复接口保持兼容；如果微信侧拒绝 @ 发送，会降级为普通群通知，避免整条告警丢失。
- 设置页保留已保存但尚未加载昵称的成员 ID，避免配置被刷新丢失；软件启动后一整个检测间隔内不会自动群发通知，避免重启误刷。

### 部署方法
1. 拉取代码后执行 `npm install`（已有依赖可跳过）。
2. 使用 `/Users/imac/Desktop/启动贾维斯.command` 或项目内 `./start-jarvis-background.sh` 启动。
3. 打开 `http://127.0.0.1:3721/brain-ui.html`，确认版本号和设置页更新。
4. 在“微信群助手”保持微信在线；如成员昵称为空，可先点击刷新昵称或等群内成员发言后再加载。

### 验证
- `node --check src/config.js src/llm-connectivity-monitor.js src/social/wechaty-duty-group.js src/api.js src/social/index.js src/ui/brain-ui/app.js src/ui/brain-ui/app-shell.js`
- `npm run test:wechat-quote-context`
- `npm run test:wechat-guard`
- `npm run test:wechat-record-all`

## v0.4.49 - 2026-05-29

### 新增
- LLM 模型设置页新增“渠道连通通知”：可开启定时检测，配置通知间隔、通知策略、要检测的 LLM 渠道和要通知到的微信群。
- 后端新增 LLM 连通性监控调度器：复用模型池真实 `chat.completions` ping 检测渠道是否可用，并记录每个 profile 的最近成功/失败状态。
- 新增手动“立即检测”和“立即检测并通知”操作，方便保存配置后立刻确认通知链路。

### 交互优化
- 通知间隔和通知策略使用大尺寸下拉框；检测渠道和通知群组使用可滚动卡片式多选列表，避免小下拉难点、看不清。
- 支持三种通知策略：异常/恢复变化通知（推荐）、只通知不通渠道、每次检测都通知。
- 微信群通知只包含渠道名称、模型、连通/失败、延迟和短错误摘要，不展示 API Key。

### 行为说明
- 定时器启动后不会在软件启动当下立刻往群里刷通知；会按配置周期自然触发。
- 未选择渠道时，默认检测当前模型池所有渠道；保存后会按勾选结果固定检测范围。
- 未选择或无法解析微信群时，检测仍会记录在设置页，但不会误发到所有群。

### 验证
- 通过 `node --check`：config、llm-connectivity-monitor、social/index、api、Brain UI app、app-shell。
- 保留并继续通过微信群引用上下文、微信群安全拦截、全量入库关键测试。

## v0.4.48 - 2026-05-29

### 新增
- 微信群 @ 回复新增“引用消息上下文”理解层：支持微信可见引用文本，以及底层 XML/元数据里的文字、图片、语音、视频、链接、小程序和表情类型识别。
- 大模型 prompt 中新增 `<wechat-quoted-message>` 精简块：只注入类型、发送者、标题/描述/URL、短摘要和引用后的当前请求，避免把原始 XML、base64、完整聊天历史塞进上下文。
- 引用图片会自动提示优先结合已入库的微信群图片解析库；引用语音若没有转写会明确说明没有语音文本，不再瞎编。

### 行为优化
- 用户引用一条消息后再 @ 助手提问时，实际请求会优先取“引用后面的当前问题”，减少模型被整段引用带偏。
- 链接/小程序/视频只按标题、描述、URL 等元数据回答；需要依据时只短引用一句，不会复述整段内容。
- Wechaty 链路会把底层消息 payload 仅用于解析引用，不直接暴露到 prompt；ClawBot 链路也会传入原始文本与消息类型。

### 验证
- 通过 `node --check`：wechat-quote-context、wechat-groups、wechaty-duty-group、wechat-clawbot。
- 新增并通过 `npm run test:wechat-quote-context`，覆盖文字引用、图片引用、链接 XML、小程序、语音、视频和无引用场景。

## v0.4.47 - 2026-05-29

### 修复 / 优化
- 优化数据库页“微信群图片解析库”的筛选控件：群组、状态、关键词、发送人、开始/结束时间全部改为大尺寸可用控件，不再出现下拉框/输入框太小看不清的问题。
- 新增“查询图片”和“重置筛选”按钮，用户不需要依赖回车或自动 change 事件。
- 图片卡片新增“编辑解析”和“删除图片”操作。

### 新增管理能力
- “编辑解析”可手动修改识图描述和标签，保存后立即刷新列表与统计。
- “删除图片”会删除图片库数据库记录，并默认尝试删除本机已入库的微信图片文件；删除接口只允许操作已入库图片的安全相对路径，不能删除任意本机文件。
- 后端新增图片库 update/delete API，均要求本机/授权访问。

### 验证
- 通过 `node --check`：wechat-image-vision、api、brain-ui app、app-shell。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过缺失 ID update/delete 安全测试。
- 通过 `git diff --check`。

## v0.4.46 - 2026-05-29

### 新增
- 数据库设置页新增“微信群图片解析库”可视化面板：可查看所有已入库微信群图片的缩略图、发送群、发送人、发送时间、解析状态、识图描述、标签和模型信息。
- 新增图片解析进度卡：显示总数、已解析、待解析、解析中、失败/无模型、base64 备份数量，并显示后台解析 worker 是否运行。
- 新增图片库筛选：按群组、解析状态、关键词、发送人、开始时间、结束时间过滤；关键词支持 `newapi/New API`、发送人别名等常见归一化。
- 新增“解析待处理”后台任务入口：点击后立即返回，不阻塞 UI；后台按批次解析待处理/失败/无模型图片。
- 数据库页打开时会自动刷新图片库；每 10 秒自动刷新解析数量和列表，并在存在待解析图片时自动触发后台补解析。

### 行为说明
- 群里新收到图片仍会按原逻辑自动入库并后台识图；本版本补齐“看得见当前解析进度、能浏览、能查询、能手动补解析”的可视化管理能力。
- 图片预览只读取 `data/wechat-media` 下已入库的微信图片相对路径，不允许任意本机路径外发或预览。

### 验证
- 真实数据库测试 `/social/wechat-groups/images`：可按 `PT站看片狂魔小群 + newapi` 查询到 Dali/力佬发过的 New API 图片，并返回解析状态与描述。
- 后台解析入口可启动 worker，并能把待解析图片数量推进。
- 通过 `node --check`：wechat-image-vision、api、brain-ui app、app-shell。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.45 - 2026-05-29

### 新增
- 微信群图片转发检索新增自然时间条件解析：支持 `今天`、`昨天`、`前天`、`大前天`、`N天前`、`5月29日`、`2026-05-29`、`上午/下午/晚上/凌晨/中午`、`9点`、`09:15`、`9点半`、`刚才/最近` 等表达。
- 图片检索现在可以把“时间 + 发送者昵称/别名 + 图片识别内容”联合打分，例如 `今天09:15力佬发的newapi图` 会优先命中当前群中对应时间附近、对应成员、内容含 New API 的图片。
- 图片检索结果返回调试用 `timeIntent`，便于后续在设置页/日志中展示机器人实际理解到的时间范围。

### 行为说明
- 群图片只要在程序运行、Wechaty 在线且已接入该群时被收到，就会自动保存入库；普通非 @ 图片也会后台识别。
- 识图成功后会保存中文描述、标签和图中文字摘录；即使识图暂未完成，只要图片文件已入库，也能按发送者和时间作为兜底候选。
- 当前仍默认只在“当前群”的图片库中转发，避免把其他群图片串发出去。

### 实测
- 对真实 `PT站看片狂魔小群` 图片库测试：`今天9点力佬发的newapi图`、`今天09:15力佬发的newapi图`、`今天上午力佬发的newapi图`、`最近力佬发的图`、`5月29日力佬发的newapi图` 均能解析时间范围并返回可用图片文件。

### 验证
- 通过 `node --check src/social/wechat-image-vision.js`。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.44 - 2026-05-29

### 修复
- 修复微信群里“给我力佬发的那张 newapi 的图”找不到图的问题：图片库搜索现在会把 `newapi`、`New API`、`New-API` 视为同一个关键词。
- 图片转发检索加入发送者昵称、花体昵称标准化和群成员常用外号匹配；当前内置兼容“力佬/大力/Dali/Dafi”这类称呼。
- 图片转发候选不再只看已经完成识图描述的图片；只要是当前群已入库且本地文件可用的微信图片，也能作为兜底候选，避免刚入库但还没识图完成时直接说找不到。

### 原因说明
- 之前机器人能“总结这张图”，是因为大模型上下文里能看到图片识图描述；但“发给我这张图”走的是另一条严格的图片转发检索链路。
- 这次 PT 群图片已入库，描述里写的是 `New API`，用户说的是 `newapi`，再加上发送者是花体昵称 `𝓓𝓪𝓵𝓲·𝓦𝓪𝓷𝓰`、用户叫“力佬”，旧检索没有做归一化和别名匹配，所以误判为没找到。

### 实测
- 对真实数据库测试 `@小风 给我力佬发的那张newapi的图`：可以命中 `PT站看片狂魔小群` 中 `𝓓𝓪𝓵𝓲·𝓦𝓪𝓷𝓰` 发过的 New API 图片，并确认本地文件可解析。

### 验证
- 通过 `node --check src/social/wechat-image-vision.js`。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.43 - 2026-05-29

### 修复
- 修复“发送给我那张图/那张山水画发我”仍被误判为生图的问题。
- 生图触发进一步收紧，避免把名词“山水画”里的“画”当作“画图”指令。

### 新增
- 新增已入库群图片转发能力：当用户要求“把那张图发给我/转发刚才那张图”时，优先在当前微信群 `wechat_group_media_items` 图片库中检索匹配图片并直接发送原图。
- 图片转发只允许发送当前群已经入库的微信图片文件，不允许发送任意本机路径，避免隐私泄露。
- 匹配逻辑会根据图片识图描述和用户请求打分，例如“水墨山水画”会命中已识别的水墨山水图片。

### 实测
- 对 `@前夜 发送给我那张水墨山水画的图片给我，给我那张图` 测试：不触发生图，命中值班群图片库中的水墨山水图，文件可解析并可发送。
- 正常生图请求仍可用。

### 验证
- 通过 `node --check`：image-generation-skill、wechat-image-vision、wechaty-duty-group。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.42 - 2026-05-29

### 修复
- 修复“看图/识图/引用图片”被误判成“生图”的问题。现在包含“看、识别、识图、图片里、图里、引用、报错、内容”等意图时，不会触发生图 Skill。
- 生图触发词收紧：只在明确“生图/画图/生成图片/画一张/绘制/设计/创作”等场景触发，不再因为“给你图/来图/引用图”误触发。
- 微信群提示词增加图片理解边界：引用图片优先使用图片记忆；若 Wechaty 只拿到 `[图片]` 引用文本而没有像素内容，会明确要求用户直接重发图片，不会擅自生图。

### 增强
- 新增从历史群聊活动记录中回填已保存图片到 `wechat_group_media_items` 的能力，可把旧的 `[媒体文件]` 图片补进识图库并生成描述。
- 已针对“值班群”这次 Hermes 截图执行回填和识图：成功导入 1 张，识别 1 张。

### 实测识图结论
- 这张 Hermes 截图可以识别到内容：图里显示 provider 180 秒无响应、重连、重试，最终 `HTTP 502: Upstream service temporarily unavailable`，并且 retries exhausted 后尝试 fallback。
- 判断更像是上游模型/中转服务不可用或超时，不是单纯 Hermes 本地逻辑问题；需要检查当前模型 `gpt-5.5`、中转 baseURL/API key/额度、上游网关稳定性和 fallback 配置。

### 验证
- 误触发测试：`我给你图 引用给你自己看`、`没让你生图 是让你识别图里的内容`、`看看图片里hermes是啥问题` 均不会触发生图。
- 正向测试：`生图 一个蓝色圆形图标`、`帮我画一张猫图` 仍会触发生图。
- 通过 `node --check`：image-generation-skill、wechat-image-vision、wechat-groups。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.41 - 2026-05-29

### UI 修复
- 修复 Skill 技能页输入框/下拉框过小的问题：生图和识图配置项现在使用正常宽度、正常高度的大表单控件。
- 生图模型不再手填，改为下拉选择内置生图模型：`gpt-image-2`、`gpt-image-1`、`dall-e-3`，并保留当前配置值。
- 识图备用模型不再手填，改为下拉选择：优先读取当前 LLM 和 LLM 模型池里的多模态候选模型，同时提供内置 GPT/Vision 模型。
- Skill 设置页加载时会同步读取 LLM 模型池，因此后续添加多模态模型后会出现在识图模型下拉框中。

### 验证
- 通过 `node --check src/ui/brain-ui/app.js`。
- 通过 `git diff --check`。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。

## v0.4.40 - 2026-05-29

### 新增：微信群识图 Skill / 图片记忆
- 新增“识图 Skill”：微信群收到图片后会保存本地文件、base64、图片元数据，并调用多模态/GPT 模型生成中文内容描述和标签。
- 新增 `wechat_group_media_items` 数据表，字段包含群、发送人、图片路径、mime、大小、sha256、base64、识图描述、标签、识图模型和状态。
- 当前 LLM 如果是多模态/GPT 模型，会优先使用当前模型识图；如果当前模型不是多模态，会自动使用备用 GPT 识图模型。
- 识图结果会进入当前微信群的图片记忆上下文；后续即使切换到 DeepSeek 等非多模态模型，也能通过图片描述理解历史图片含义。
- 群聊数据库混合搜索增加图片描述搜索。
- 数据库备份导出包含 `wechat_group_media_items`，图片 base64 和描述可随备份恢复。

### 设置页
- Skill 技能页新增“识图 Skill”配置：启用开关、优先当前多模态模型、备用 GPT Base URL、备用模型、备用 Key、识图超时、状态刷新。
- 状态显示真实图片入库数、已描述数、待处理数、base64 保存数。

### 行为说明
- 被 @ 的图片消息会优先等待识图结果再进入大模型回复，保证当场能理解图片。
- 普通群图片会后台识图，不打扰群聊；识图结果会进入后续记忆上下文。
- 不直接把本机文件发给群友；图片理解只用于本地知识库和回复上下文。

### 验证
- 实测本地测试图片成功入库 base64 并识别：蓝色圆形、白色背景、极简图形。
- 实测识图耗时约 13 秒，使用当前多模态模型 `gpt-5.4`。
- 通过 `node --check`：config、wechat-image-vision、wechaty-duty-group、wechat-groups、api、database-overview、brain-ui app。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.39 - 2026-05-29

### 优化
- 数据库页的“微信群成员/昵称”不再直接显示历史身份记录总数，改为显示按“群名 + 昵称”聚合后的有效昵称数。
- 新增“成员有效视图”面板：同时展示有效昵称、历史身份记录、可合并历史记录、可用 wxid 数。
- 按群展示成员昵称聚合结果，例如“PT站看片狂魔小群：52 个昵称 / 358 条历史身份”。
- 展示重复昵称示例，方便排查 Wechaty 重登后 sender_id 变化造成的虚高。

### 说明
- 本版本不删除原始 `wechat_group_member_names` 记录，只做安全显示层合并，避免误删历史身份和昵称映射。
- 原始表仍完整保留，导出备份仍包含全部历史记录。

### 验证
- 当前实测：有效昵称约 359，历史身份记录 1717，可合并历史记录 1358，wxid 可用数 0。
- 通过 `node --check src/database-overview.js`。
- 通过 `node --check src/ui/brain-ui/app.js`。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.38 - 2026-05-29

### 修复
- 取消微信群统计总结的“启动/登录自动补发”：程序启动、微信扫码登录、恢复连接后，不再立刻向群里发送阶段总结。
- 定时总结调度器启动时会记录当前 interval 周期，并跳过该启动周期，避免一登录就触发发送。
- 删除启动后 15 秒延迟检查发送逻辑；总结只会在用户手动点击发送，或进入后续真正的定时周期时发送。

### 保留行为
- 设置页里的“手动发送总结”仍然可用。
- 如果用户开启了定时总结，程序不会登录即发，而是等下一个定时周期再按配置发送。
- 每日 0 点统计仍受设置页开关和时间控制。

### 验证
- 通过 `node --check src/social/wechat-group-digest.js`。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check`。

## v0.4.37 - 2026-05-29

### 里程碑：微信群长期记忆 / Honcho / 本地向量可用化
- Honcho 本地服务已接通并实测健康：`http://127.0.0.1:8018/health` 返回正常。
- 微信群逐条聊天记录现在先写入本地耐久库，再同步 Honcho；Honcho 不通时不会丢消息、不会失忆。
- 已把本地 1011 条微信群消息同步到 Honcho，当前待同步 0 条。
- 已为核心长期记忆与微信群聊天记录补齐本地轻量向量：核心 180/180，群聊 1011/1011。未配置云端 embedding 时也能做本地语义检索。
- 新增从历史群聊中提取“每群/每成员”长期记忆的能力，并已从现有聊天中提取出成员称呼/身份类记忆。

### 数据库菜单增强
- 数据库页新增「补齐向量」「提取成员记忆」「同步 Honcho」三个一键操作。
- 新增聊天记录 / 长期记忆混合搜索框，可同时搜索微信群逐条聊天记录、群组记忆、成员记忆。
- 数据库状态卡真实显示 Honcho 连通状态、已同步/本地消息数量、核心/群聊/群记忆向量化进度。
- 导出备份 JSON 现在会正确保存 BLOB 向量字段为 base64；导入时会恢复 BLOB，并按聊天内容/记忆内容/mem_id 去重，避免重复灌库。

### 回答记忆逻辑
- 核心记忆注入器增加本地向量兜底：没有云端 embedding 配置时，也会使用本地轻量向量召回补充上下文。
- 微信群回答上下文会优先合并 Honcho 长期记忆、Honcho 摘要、Honcho 最近消息；Honcho 读取失败时自动降级到本地群记忆和本地向量检索结果。

### 验证与实测
- Honcho Docker 服务：api/database/redis healthy，deriver running。
- 历史同步：1011 条本地微信群消息同步 Honcho 成功，错误 0。
- 成员记忆提取：扫描 1011 条，提取 4 组成员/群组记忆，错误 0。
- 通过 `npm run test:wechat-guard`。
- 通过 `npm run test:wechat-record-all`。
- 通过 `git diff --check` 和关键文件 `node --check`。

## v0.4.36 - 2026-05-29

### 修复
- 清理并更正错误记忆 `fact_user_wechat_groups_18`：18 个微信群是历史错误记忆/历史 ID 混淆，不再作为事实使用。
- 清理导致上下文污染的临时 focus 栈片段，降低模型反复进入“高风险拒绝”状态的概率。
- 兜底回复新增供应商安全拒绝过滤：不会再把 `The request was rejected because it was considered high risk` 这类英文内部拒绝原文直接发给用户。

### 新增
- 设置页新增独立「数据库」菜单。
- 数据库页集中展示：总占用、微信群聊天记录条数/容量、微信群知识库/记忆、核心长期记忆、成员昵称表、图片/媒体文件占用。
- 新增表级明细，按估算占用从大到小显示表名、行数和容量。
- 新增 `GET /settings/database` 后端接口，用于读取本机数据库和知识库容量统计。

### UI 优化
- 数据库/知识库相关信息从微信群助手页拆出，避免微信群助手设置继续变得过乱。
- 新数据库页以“总占用”为主视图，常用数量用卡片展示，细节放到可滚动表格里。

### 当前实测数据
- 本机总占用约 39 MB。
- 微信群聊天记录约 1010 条。
- 核心长期记忆约 180 条。
- 微信群成员/昵称记录约 1002 条。
- 图片/媒体文件约 29 MB。

### 验证
- 通过 node --check：src/index.js、src/database-overview.js、src/api.js、src/ui/brain-ui/app-shell.js、src/ui/brain-ui/app.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.35 - 2026-05-29

### 新增
- LLM 模型池每个模型卡片新增「测试连通」按钮。
- 点击后后端会用该模型发起一次轻量聊天补全检测，成功后状态变为绿色「连通」，失败后状态变为红色「不通」并显示错误原因。
- 测试过程中按钮会显示「测试中…」，完成后显示连通耗时。

### 后端
- 新增 `POST /settings/llm-profile/test` 接口。
- 测试成功会更新 `lastSuccessAt` 并清空 `lastError`；测试失败会更新 `lastFailedAt` 和 `lastError`，但不会把模型加入自动冷却，避免手动检测影响故障切换策略。

### 验证
- 通过 node --check：src/config.js、src/api.js、src/ui/brain-ui/app.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.34 - 2026-05-29

### 优化
- LLM 模型池新增直观连通状态显示。
- 模型卡片的「状态」改为「连通状态」，使用信号条图标区分：绿色=连通，红色=不通，黄色=冷却中，灰色=未知/已关闭。
- 如果最近失败时间晚于最近成功时间，会显示红色「不通」，并继续保留上次错误文本，便于判断哪个模型不可用。

### 验证
- 通过 node --check：src/ui/brain-ui/app.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.33 - 2026-05-29

### 优化
- 实测生图速度：low / 1024×1024 两次分别约 84 秒、75 秒。
- 将生图 API 默认超时从 90 秒提高到 180 秒，避免服务稍微排队就被误判为超时。
- Skill 设置页新增「API 超时」下拉框，可选 120 / 180 / 240 / 300 秒。
- 生图超时错误现在会明确提示当前超时时间，例如 `图片生成请求超时（180 秒）`。

### 建议
- 默认 low / 1024×1024 继续保持速度优先。
- 普通生图建议 180 秒；高清/2K/4K/8K 如果后续仍超时，可在 Skill 设置里调到 240 或 300 秒。

### 验证
- 通过 node --check：src/social/image-generation-skill.js、src/social/wechaty-duty-group.js、src/config.js、src/api.js、src/ui/brain-ui/app.js、src/ui/brain-ui/app-shell.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.32 - 2026-05-29

### 修复
- 修复微信群生图触发词过窄的问题：`生成一张赛博朋克风格的白龙马头像` 这类自然表达现在会命中生图 Skill。
- 生图触发范围从短距离 `生成...图片` 放宽为更符合人话的 `生成/画/设计/创作...图片/头像/壁纸/海报/插画/logo/图标`。
- 避免生图请求落入普通 LLM 工具链后让模型自行 curl API，导致只得到 base64、不发送图片、甚至 180 秒 watchdog 超时。

### 安全
- 已清理本机日志中此前由模型自行 curl 暴露出的 API Key 明文，替换为 `sk-***REDACTED***`。

### 验证
- 已验证 `@前夜 生成一张赛博朋克风格的白龙马头像`、`生图 一个白龙马头像`、`画一个未来城市壁纸` 均命中生图 Skill。
- 通过 node --check：src/social/image-generation-skill.js、src/social/wechaty-duty-group.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.31 - 2026-05-29

### 新增
- 新增「Skill 技能」设置菜单，首个技能为「生图 Skill」。
- 生图 Skill 支持配置 Base URL、模型、API Key、每人每小时限额、默认质量、高清质量。
- 微信群里有人 @ 助手并明确要求“生成图片 / 生图 / 画图 / 出图”时，会直接调用生图 API 生成图片并发送到群里。
- 生图调用不添加任何预制提示词，只使用群友提出的图片需求文本。
- 新增每人每小时限流：默认每人每小时最多 10 张图，超过后会 @ 提问人反馈。
- 默认使用 low 质量和 1024×1024 分辨率以提高速度；用户明确要求高清、2K、4K、8K、超清时使用高质量参数。

### 接入
- 生图模型：gpt-image-2。
- OpenAI 兼容接口：/images/generations。
- 支持 API 返回 URL 或 b64_json；生成结果会保存到本机 data/generated-images 后再作为图片发送到微信群。
- API 调用失败、超时、未配置密钥、限流等情况都会 @ 提问人说明原因。

### 安全
- API Key 只写入本机运行配置，不提交到 GitHub，不在设置页回显明文。
- 生成图片发送使用本机刚生成的图片文件，不允许群友指定本机文件路径外发。

### 验证
- 本机已用 low/1024×1024 实测生成成功，并保存图片到 data/generated-images。
- 通过 node --check：src/social/image-generation-skill.js、src/social/wechaty-duty-group.js、src/config.js、src/api.js、src/ui/brain-ui/app.js、src/ui/brain-ui/app-shell.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.30 - 2026-05-29

### 新增
- 尝试采集微信群成员稳定微信身份字段：成员库新增 `wechat_id`、`wxid`、`stable_key`、`raw_identity` 字段。
- 管理员识别优先级调整为：精确 sender_id > 稳定微信身份（wxid/微信号 Alias）> 当前群成员快照唯一昵称兜底。
- 对单个群成员额外尝试调用 wechat4u `batchGetContact({ UserName, EncryChatRoomId })` 拉取详情，测试是否能拿到真实微信号或 wxid。

### 修复
- 修复昵称兜底把历史旧 sender_id 也算入同名人数的问题：现在只检查当前最新群成员快照里的同名人数，避免重登后历史记录造成误判。
- 如果当前快照里出现多个同名成员，昵称兜底会拒绝授权，避免普通群友改成管理员同名后冒充。

### 实测结果
- 当前 wechat4u 在 `值班群` 中只能拿到临时 `UserName=@...`，未暴露 `wxid` 或微信号 Alias；数据库字段已保留，若后续协议/账号能返回会自动启用。

### 验证
- 通过 node --check：src/social/wechaty-duty-group.js、src/social/wechat-group-stats.js、src/social/dispatch.js、src/social/wechat-groups.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.29 - 2026-05-29

### 修复
- 修复 Wechaty 重新登录后管理员 sender_id 变化导致管理员权限失效的问题。
- 当历史已选管理员 ID 在同一个群内对应的微信昵称/群昵称与当前发言人一致时，自动识别为同一管理员并补录新的 sender_id。
- 解决管理员请求查看性格预设提示词时，程序界面已经生成内容但微信发送层仍提示 `local_file_reference_in_wechat_outbound` 的问题。

### 说明
- Wechaty Web 协议下 sender_id 可能随登录态变化；本版增加同群历史管理员昵称兜底，避免每次扫码后管理员权限丢失。
- 首次命中兜底时日志会输出 `[WechatyAdmin] 管理员 sender_id 已随登录变化，按同群昵称匹配自动补录`，之后会恢复精确 sender_id 判断。
- 普通群成员仍不能靠自称管理员绕过；必须先存在历史已选管理员记录，且在同一个群内匹配到对应昵称。

### 验证
- 通过 node --check：src/social/wechaty-duty-group.js、src/social/dispatch.js、src/social/wechat-groups.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.28 - 2026-05-29

### 修复
- 修复已验证微信群管理员仍被“本机隐私/安全黑名单”发送层拦截的问题：管理员消息在入口安全检查通过后，现在会把 `wechat_admin` 标记继续传递到 Wechaty 发送层。
- Wechaty 群消息发送层新增管理员绕过判断：只有普通群成员会触发本机文件、桌面图片、file:// 路径等外发拦截；已验证管理员按管理员权限执行。
- 优化管理员模式提示词：明确普通群成员安全边界、媒体/本机隐私拒绝话术、黑名单限制不适用于已验证管理员；管理员可以查看性格预设、微信群助手配置、安全规则摘要、记忆状态等可读内容。

### 安全边界
- 管理员绕过只基于设置页保存的微信 sender_id 精确匹配，不接受昵称、自称或群备注伪造。
- 默认仍会要求模型隐藏 API Key、Token、密码、Cookie、私钥等密钥原文，避免误把真正密钥发到微信群。

### 验证
- 通过 node --check：src/social/wechaty-duty-group.js、src/social/dispatch.js、src/social/wechat-groups.js。
- 通过 npm run test:wechat-guard。
- 通过 npm run test:wechat-record-all。

## v0.4.27 - 2026-05-29

### 修复
- 修复表情包搜索总是发送同一张的问题：表情搜索结果现在在高质量候选池中按随机种子打散，不再永远取第一张。
- 修复明确“发/来/整 表情包、斗图、梗图、GIF”等指令有时被模型文本回复的问题：Wechaty 群消息入口新增直发表情包分支，命中后直接搜索并发送图片/GIF，不再等待大模型自由发挥。
- 直发表情包仍遵守安全规则：只发送 HTTPS 公开图片/GIF，不发送本机文件，不显示 URL 文本。

### 验证
- 已验证相同关键词在不同 seed 下返回不同首图。
- 通过 npm run test:wechat-record-all。
- 通过 npm run test:wechat-guard。

## v0.4.26 - 2026-05-28

### 修复
- 彻底修复斗图仍发送裸 URL 的问题：现在同时剥离 Markdown 图片、Markdown 链接和纯 URL。
- 如果剥离 URL 后只剩一个 @ 昵称，也不会再发送文字气泡，直接发送图片/GIF。
- 新增内部剥离逻辑验证，确认 `@用户 https://...gif` 会变成纯图片发送。

### 验证
- 通过裸 URL 剥离测试。
- 通过 npm run test:wechat-record-all。
- 通过 npm run test:wechat-guard。

## v0.4.25 - 2026-05-28

### 修复
- 修复微信群斗图发送时先显示图片/GIF URL 链接的问题：现在默认隐藏 URL 文本，只直接发送图片或 GIF。
- 如果 AI 回复内容只有表情包 URL，则不再发送任何文字气泡。
- 如果 AI 同时写了自然语言说明和表情图，则只发送说明文字 + 图片，不暴露链接。
- 优化图片/GIF发送速度，图片发送改为并发投递并统计成功数量。

### 验证
- 通过 node --check。
- 通过 npm run test:wechat-record-all。
- 通过 npm run test:wechat-guard。

## v0.4.24 - 2026-05-28

### 新增
- 新增 AI 斗图表情包能力，接入慕名 API / xiaoapi 表情搜索接口。
- 新增 meme_search 工具，AI 可按“斗图、表情包、梗图、无语、鄙视、笑死、吃瓜”等请求搜索公开网络图片/GIF。
- 新增微信群助手「AI 斗图表情包」设置区，可开启/关闭、选择表情源、设置每次发送数量、冷却时间，并支持关键词测试预览。

### 安全与边界
- 仅发送 HTTPS 公开网络图片/GIF，不做微信原生表情包收藏/表情商店能力。
- 默认只允许 biaoqing.gtimg.com、tugelepic.mse.sogou.com 两类表情图域名。
- 继续禁止读取、上传、转发或描述本机文件、桌面图片、截图、相册、file:// 路径。
- API 失败时返回错误给 AI，不阻塞正常文字回复。

### 验证
- 已验证 xiaoapi meme 搜索“鄙视”可返回 GIF 图片。
- 通过 npm run test:wechat-record-all。
- 通过 npm run test:wechat-guard。

## v0.4.23 - 2026-05-28

### 修复
- 新增微信群助手掉线检测机制：登录态恢复超时、logout、连接错误、健康检查失败会自动标记为离线。
- 新增掉线提醒：离线后通过系统通知/窗口提示/SSE 状态事件提醒用户重新扫码。
- 设置页状态改为真实显示：缓存群明确显示为“不可接收 @ 消息”，不再误导为在线。
- 后端状态接口新增 connection_state 与更准确的 needs_relogin，便于前端和用户判断真实可用性。

### 说明
- 只有 online=true 且 connected 且当前进程真实解析到群，才显示“已真实连接”。
- 微信掉线后不会再把历史群缓存当作可用群消息通道。

## v0.4.22 - 2026-05-28

### 修复
- 修复微信群列表重复显示的问题：同一个微信群在 Wechaty 重新登录后可能产生多个历史 room_id，现在按群名归并，只显示一个真实群。
- 修复重复群影响微信助手 @ 回复设置、群统计与定时总结、Honcho 群记忆管理、聊天记录群选择等页面的问题。
- 已识别群接口保留 historical_ids/duplicate_count 供排查，但 UI 不再展开历史旧 ID。

### 说明
- 新增群仍不会自动开启 @ 回复，需要在微信助手中手动勾选并保存，避免误回复。

# BaiLongma v0.4.21 备份说明（2026-05-28）

## 版本定位

- 版本号：`v0.4.21`
- 维护仓库：`https://github.com/xiaoguiwucan/BaiLongma`
- 里程碑：微信群助手可用版 + Honcho 群记忆管理 + 性格预设 + 安全黑名单 + 网络梗/称呼记忆 + 网络图片发送安全边界 + 全量群统计/排行榜/定时总结 + 手动选择统计群组修复 + 聊天记录库可视化/导入导出 + 昵称强制刷新修复 + 多群统计区分 + 多人 @ 排队 + 管理员模式 + LLM 多模型池/自动故障切换版，并修复微信群 @ 回复错人、Wechaty 回复目标解析链路、聊天记录库持续入库、Wechaty 启动卡住自恢复、一直“跳过识别”不回复，以及后台记忆识别/整理内部 skip 工具循环刷屏。
- 核心结果：用户已经测试确认，微信群里 @ 扫码登录的微信账号后可以正常回复；本版本在全量群消息统计基础上新增“微信群聊天记录库”，可以按群查看所有已入库聊天记录、完整时间、消息总数、成员昵称、类型/关键词/时间筛选，并支持 JSON/CSV 导出和 JSON 导入；新收到的图片/表情/音视频等媒体会尝试保存到本机数据目录并可在记录库预览/备份；本版本进一步修复微信群昵称仍显示“未知成员”的问题，支持手动刷新昵称并按群名合并重新登录后的新旧 room_id；v0.4.5 在此基础上新增多人 @ 排队回复、多群统计总览、排行榜来源群标注、精确 sender_id 管理员模式和榜单自动刷新；v0.4.6 新增 LLM 多模型池与自动故障切换，在当前模型没额度/限流/不可用时自动切到备用模型；v0.4.7 修复微信群 @ 回复错人，按真实提问人精确 @ 回去；v0.4.8 修复 `wechaty:room:<room>:member:<member>` 在分发层未拆分导致 room_id 错误的问题；v0.4.9 修复聊天记录库受统计/日报群组勾选影响导致不持续入库的问题；v0.4.10 修复 Wechaty 启动卡在 starting 导致收不到群消息的问题；v0.4.11 修复记忆识别器内部 `skip_recognition` 污染主对话导致真实 @ 消息被跳过的问题；v0.4.12 进一步修复后台记忆识别器/整合器自身反复调用 `skip_recognition/skip_consolidation` 导致刷屏和熔断的问题；v0.4.13 清理单次后台 skip 日志和 TICK 运行时工具触发的无意义记忆识别；v0.4.14 修复微信群重复回复和“已回复/回复完毕/本轮结束”等内部状态外发；v0.4.15 修复微信群聊天记录页“不更新”的误判和真实刷新缺陷，新增查看群组下拉框、结束时间自动跟随当前时间、自动刷新聊天记录和 DB 最新入库时间提示；v0.4.16 修复微信群回答不查聊天记录库导致“记不完整”，@ 回复时会按当前群从 SQLite 聊天流水检索证据；v0.4.17 修复微信群 @ 错人、管理员勾选丢失、管理员昵称搜索添加和管理员保护回怼；v0.4.18 修复微信群发送失败重试变慢，并新增多人 @ 并行批处理；v0.4.19 优化管理员设置页为昵称显示/昵称搜索，底层仍精确 ID 授权；v0.4.20 修复 Honcho 离线影响设置页和 LLM 模型操作；v0.4.21 统一新增微信群在各设置模块的显示来源。

## 本版本包含的关键能力

1. 微信群助手独立设置菜单。
2. Wechaty 扫码登录/恢复登录。
3. 获取真实微信群列表并支持勾选多个群组。
4. 只有群里 @ 当前登录账号才调用大模型，普通群消息不主动打扰。
5. 回复时 @ 原提问人，并避免把内部联系人 ID 直接显示到群里。
6. Honcho 群知识库配置入口：每个微信群独立 session/peer。
7. Honcho 群记忆管理 UI：按群查看原始消息、自动摘要、长期结论；支持手动添加长期记忆、删除结论、清空本群 session。
8. 微信群助手性格设定：支持手动编辑提示词，并内置 3 个一键预设。
9. 群指令安全守卫：拒绝查看/列出本机文件、读取凭证、执行命令、控制电脑、账号资金等高危请求，设置页展示 19 类黑名单规则。
10. ClawBot 群聊路径也接入安全守卫，避免旁路绕过。
11. 网络梗理解：v我50 / vw50 / 疯狂星期四等会按中文群聊梗处理。
12. 群媒体边界：允许公开网络图片/表情包链接，禁止本机文件、桌面文件、截图、相册外发。
13. 显式称呼/身份记忆：群成员说“以后叫我大哥 / 我是你大哥 / 我叫xxx”会即时写入 Honcho 本群长期记忆。
14. Honcho 记忆详情分开展示群组长期记忆和成员长期记忆，成员记忆仅在当前群内可用并与群组记忆共同匹配。
15. 微信群助手性格设置具备独立保存按钮和明显生效状态。
16. Mac Electron 启动脚本与 Dock 显示修复。
17. 火山/豆包 ASR、TTS 自回声、整段播报等语音链路同步修复。
18. 修复微信群偶发回复英文内部协议文本的问题，内部工具执行状态不会再发到群里。
19. 微信群全量活动统计：记录所有已接入群的文字、图片、表情、链接、小程序等消息。
20. 群排行榜：发言榜、发图榜、表情榜、链接榜、装逼榜（启发式娱乐统计）。
21. 定时总结：支持阶段总结和每日 00:00 群聊日报，自动去重避免重复发送。
22. 设置页新增“群统计与定时总结”面板，可以手动保存配置、刷新统计、立即发送本群总结.
23. 群统计排行榜优先显示微信群昵称/备注/微信昵称，不再把 `@a5a383...` 这类 WeChaty 内部 ID 直接显示出来。
24. Wechaty 接入群或收到消息后会后台刷新群成员昵称，并自动回填旧统计表里的内部 ID。
25. 微信群聊天记录库：按当前群查看本机 SQLite 已入库消息总数、完整时间、成员昵称、内容、图片/表情/链接/装逼统计。
26. 聊天记录筛选：支持开始时间、结束时间、消息类型、关键词检索和分页加载更多。
27. 聊天记录导出：支持 JSON 完整备份和 CSV 表格导出。
28. 聊天记录导入：支持 JSON 恢复，重复记录会跳过；JSON 中包含的媒体文件会恢复到本机数据目录。
29. 媒体留存：新收到的图片、表情、音频、视频、附件会尝试保存到 `data/wechat-media/`，设置页可以预览图片/播放音视频。
30. 媒体安全：记录库媒体预览只允许读取已入库相对路径，不能借此读取任意本机文件。
31. 昵称强制刷新：直接调用 wechat4u `batchGetContact` 拉取群成员昵称，解决 Wechaty 默认 API 只能拿到“未知成员”的问题。
32. room_id 变化合并：重新扫码后微信群 room_id 变化时，统计和聊天记录按群名合并，不再拆成两个群。
33. 聊天记录库 UI 优化：主查询按钮、今天快捷按钮、刷新昵称按钮、清晰筛选输入框和用途说明。
34. 多人 @ 排队：微信群 @ 消息不再按同群覆盖旧消息，也不会打断正在回复的用户消息，按到达顺序逐条处理。
35. 多群统计总览：统计页可切换“当前群 / 已选统计群总览”，多群排行榜每行显示来源群。
36. 管理员模式：只按 Wechaty sender_id 精确识别管理员，可从成员 ID 列表点选；管理员 @ 可跳过微信群黑名单，普通成员仍严格拦截。
37. 榜单自动刷新：设置页停留在微信群助手时每 12 秒刷新统计榜单，保存统计群后立即刷新。
38. LLM 多模型池：设置页可保存多个模型 profile，支持名称、Provider、模型、API Key/自定义端点、启停、排序、编辑、删除和设为当前。
39. LLM 自动故障切换：额度不足、余额不足、限流、认证失败、模型不可用、服务端 5xx 或网络超时时，尚未输出内容前自动切换备用模型，并记录失败冷却。
40. LLM 安全展示：设置页和 `/settings` 只显示 Key 是否已配置与尾号，不返回明文 API Key。
41. 微信群 @ 修复：每条群消息生成独立回复目标，按当前提问人 sender_id / sender_name 精确 @，不再误 @ 管理员或上一位成员。
42. Wechaty 回复目标解析热修复：`wechaty:room:<room>:member:<member>` 会被正确拆成 room_id 和 member_id，发送时不会把 `:member:` 拼进 room_id。
43. 聊天记录库持续入库：原始微信群流水不再受统计/日报 selectedGroups 影响，只要 Wechaty 收到群消息就写入本机 SQLite；非接入群只入库，不进入 Honcho/LLM/自动回复。
44. Wechaty 启动自恢复：启动 60 秒没有二维码/登录/真实在线状态时自动重启连接，手动“登录/恢复微信”也能打破假 starting。
45. 跳过识别污染修复：记忆识别/整理内部工具不会再进入主对话工具列表，真实微信群 @ 消息必须按主对话回复。
46. 后台 skip 循环修复：记忆识别器/整合器遇到内部终止工具后立即结束，TICK 空闲心跳不再进入记忆识别，内部记忆工具不写入审计流也不在前端思考流展示。
47. 后台 skip 显示清理：内部记忆工具不再输出工具调用日志；TICK 只做节奏/界面等运行时动作时不再调用记忆识别器。
48. 微信群重复回复修复：微信群 @ 一次成功发送后本轮立即结束；内部结束语不允许发到群里；已成功回复后后续超时不再重排队。
49. 聊天记录库刷新修复：记录库新增查看群组下拉框；默认结束时间随查询自动推进到当前时间；设置页停留时自动刷新记录列表；摘要显示当前查看群和 DB 最新入库时间，避免选错群或旧时间筛选造成“不更新”误判。
50. 聊天记录库回答检索：微信群 @ 回复会按当前群从 `wechat_group_activity` 检索关键词、称呼和历史发言证据，回答“谁说过/老登是谁/称呼关系/之前记录”时优先基于数据库。
51. 微信群 @ 目标底层锁定：send_message 在 Wechaty 群消息上下文下强制使用本轮真实提问人 sender_id，模型不能把回复 @ 到被讨论对象。
52. 管理员模式修复：管理员勾选不再被状态轮询清掉；支持按微信昵称/群名/ID 搜索添加；普通群友暗算管理员时会保护性回怼。

53. 微信群发送失败修复：真实 sender_id 回复目标会临时加入本轮可发送/可见白名单，避免模型传错 target_id 后先失败再重试。
54. 多人 @ 并行处理：短时间多名群友 @ 时默认最多 3 条并行调用同一套回复逻辑，仍分别按真实 sender_id @ 回提问人。
55. 管理员设置昵称化：设置页显示和搜索微信昵称，成员卡片隐藏长 ID；后台继续保存精确 sender_id，兼顾好用和安全。
56. Honcho 离线降级：Docker/Honcho 未启动时群记忆接口返回降级状态，不再影响 LLM 模型编辑、设为当前和删除操作。
57. 新增群显示统一：设置页合并 Wechaty 缓存群、成员昵称库和聊天记录库群，新增群会出现在 @ 回复、记忆、记录库和统计候选中。

## 从 GitHub 恢复部署

```bash
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
git checkout v0.4.21
npm install

# 推荐：Mac 桌面启动
./start-jarvis.command

# 或者普通 Electron 启动
npm start
```

启动后打开设置页：

1. 进入“微信群助手”。
2. 点击登录/恢复微信，按二维码扫码。
3. 等待显示已登录/在线状态。
4. 刷新或等待自动获取群组列表。
5. 勾选需要接入的群组，例如“值班群”“PT站看片狂魔小群”。
6. 在「微信群助手性格设定」选择预设：主人数字分身、技术值班助手、幽默社交助手；也可以手动微调文本。
7. 点击“保存并生效”。
8. 到微信群里 @ 刚扫码登录的微信账号进行测试。



## v0.4.6 本次更新内容

- 新增 LLM 模型池：`设置 -> LLM 模型` 可添加多个模型配置，不再只能保存一个 Provider/模型。
- 每个模型配置支持自定义名称、Provider、模型、API Key、自定义 Base URL、启用/关闭、编辑、删除、上移/下移优先级、设为当前。
- 自动故障切换默认开启：当前模型没额度、余额不足、限流、认证失败、模型不可用、5xx 或网络超时时，会按优先级切换下一个启用模型。
- 只在回答尚未输出内容时切换，避免回复重复、语音播报断裂或内容拼接错乱。
- 失败模型会写入 `lastError/lastFailedAt/cooldownUntil`，冷却期间自动跳过，避免每轮都打到没额度的模型。
- 旧版单模型配置会自动迁移为模型池第一项，原 `/activate` 和 `/settings/model` 仍兼容。
- 新增 API：`POST /settings/llm-profile`、`POST /settings/llm-profile/select`、`POST /settings/llm-profile/delete`、`POST /settings/llm-failover`。
- `/settings` 返回 `llm.profiles`、`llm.activeProfileId`、`llm.failover`，但不会返回明文 API Key。
- Brain UI 会在模型切换时显示“当前模型不可用，正在无缝切换到备用模型”的状态提示。

使用方式：更新重启后进入 `设置 -> LLM 模型`，至少添加两个启用模型。把常用模型放在最上方，把备用模型排在后面；保持“额度不足/限流时自动切换备用模型”开启。

## v0.4.5 本次更新内容

- 修复多人同时 @ 会吞掉前一条回复的问题：微信群 @ 消息进入队列时带 `noPrune/noPreempt`，不再被同群后一条消息覆盖，也不再打断正在处理的用户消息。
- 群统计页新增“查看当前群 / 已选统计群总览”切换。
- 多群总览中发言榜、发图榜、表情榜、链接榜、装逼榜每行都会显示来源群名。
- 新增“按群拆分”概览，多个群的统计不会混在一起看不懂。
- 新增管理员模式设置区：启用开关、管理员 sender_id 输入框、最近成员 ID 列表、一键加入管理员。
- 管理员识别只使用 Wechaty `sender_id` 精确匹配，不使用昵称、备注或群里自称。
- 管理员 @ 时跳过微信群黑名单；普通群成员继续受到危险指令库保护。
- 新增成员 ID API：`GET /social/wechat-groups/members`。
- 榜单在设置页打开并停留在“微信群助手”时每 12 秒自动刷新；保存统计群组后立即刷新。

使用方式：更新重启后进入 `设置 -> 微信群助手`。需要管理员时先点击“刷新成员 ID”，从成员列表点选自己的微信 sender_id，再启用“管理员模式”并保存。多群统计在“群统计与定时总结”里先勾选群并保存，再把查看范围切到“已选统计群总览”。



## v0.4.4 本次更新内容

- 修复微信群成员昵称仍显示“未知成员”的问题：新增 wechat4u `batchGetContact` 群成员资料刷新。
- 新增“刷新昵称”按钮和 API：`POST /social/wechaty-duty-group/refresh-members`。
- 本机验证：强制刷新返回 `rooms=3, members=59, named=59`，值班群成员“风/小号/移动小号”等昵称已写入映射表。
- 统计/聊天记录查询按群名合并，避免重新扫码后 room_id 变了导致旧记录和新记录分裂。
- 后端统计入库判断兼容旧 room_id 选择，已选过的群重新扫码后仍能继续入库。
- 聊天记录库筛选区 UI 重做：主查询按钮、今天快捷按钮、刷新昵称按钮、清晰日期输入框。
- 页面新增解释：聊天记录库是原始消息流水；群记忆管理是 Honcho 提炼后的长期知识。

使用方式：更新并重启后，进入 `设置 -> 微信群助手 -> 微信群聊天记录库`，点击“刷新昵称”，再点“查询记录”。


## v0.4.3 本次更新内容

- 新增“微信群聊天记录库”面板，直接展示当前选中群的全量本地统计记录。
- 记录行显示 `YYYY-MM-DD HH:mm:ss` 完整时间、成员昵称、消息类型、内容、媒体标记和 sender_id/source 追踪信息。
- 顶部摘要显示已入库总条数、当前显示条数、参与人数、图片/表情/链接总数。
- 支持按开始时间、结束时间、类型和关键词筛选，支持“加载更多”分页。
- JSON 导出包含 records、统计范围、数据库路径、媒体 base64 备份；CSV 导出方便表格查看。
- JSON 导入会恢复记录和媒体文件，并跳过重复记录。
- 新增媒体预览接口与 UI：本机已保存图片可直接预览，音视频可播放，其他媒体可打开。
- 修复时间筛选只有分钟导致边界记录丢失的问题；图片/表情/链接筛选会包含混合消息。
- 排行榜与记录库统一读取成员昵称映射，后续真实在线刷新成员后旧记录会逐步从内部 ID 变成昵称。

使用方式：更新并重启后，进入 `设置 -> 微信群助手`，选择一个已勾选统计/总结的群，在“微信群聊天记录库”中点击“查询记录”。需要备份时点击“导出 JSON”；需要表格查看时点击“导出 CSV”。


## v0.4.2 本次更新内容

- 修复微信群发言榜、发图榜、表情榜、链接榜、装逼榜显示内部 ID 的问题。
- 成员显示名解析顺序：微信群昵称/群备注 -> 微信备注/别名 -> 微信昵称 -> “未知成员”。
- 接入群或收到消息时后台刷新群成员列表，并回填 `wechat_group_activity` 中已有的内部 ID 记录。
- 排行榜按 `sender_id` 合并统计，避免同一个成员因昵称变化被拆成多条排行。
- 最近统计记录、链接列表和群总结重点线索同步清洗成员昵称。

使用方式：更新并重启后，保持微信群助手在线；进入已勾选群或等待该群新消息触发成员刷新，排行榜会逐步从内部 ID 变成微信昵称。


## v0.4.1 本次更新内容

- 群统计与定时总结新增独立群组选择区域，必须手动勾选并保存。
- 未选择的群不会写入本地统计库，也不会自动发送阶段总结或每日统计，避免误发所有群。
- 统计面板显示数据位置：本机 SQLite `data/jarvis.db` 的 `wechat_group_activity` 表。
- 统计面板新增“本地统计库最近记录”，可以直观看到新写入的统计消息。
- Honcho 记忆详情固定展示“群组长期记忆”和“成员长期记忆”两个分区，即使为空也说明原因。
- 历史英文内部协议误回复在 Honcho 展示和上下文注入中隐藏，避免继续污染记忆。

使用方式：进入 `设置 -> 微信群助手 -> 群统计与定时总结`，勾选参与统计/定时总结的群组，然后点击“保存总结设置”。


## v0.4.0 本次更新内容

- 修复微信群里偶发回复 `I did not actually call the required tool...` 英文内部协议文本的问题。
- 主循环兜底改为使用微信群原始 `user_text` 判断工具意图，避免完整 prompt 中的安全说明误触发“需要工具”。
- 新增 `wechat_group_activity` 统计表，记录所有已接入群的文字、图片、表情、链接、小程序和来源。
- 新增 `wechat_group_digest_sent` 去重表，避免阶段总结或每日统计重复发同一个群。
- 新增发言榜、发图榜、表情榜、链接榜、装逼榜；装逼榜为娱乐向启发式统计。
- 新增定时总结调度：阶段总结可选 30 分钟、1 小时、3 小时、6 小时、12 小时、每天一次；每日统计默认 00:00。
- 设置页新增“群统计与定时总结”面板，可保存自动总结配置、刷新今日统计、手动立即发送本群总结。
- Honcho 成员记忆读取从消息 metadata 反推成员 peer，解决成员长期记忆显示不稳定的问题。
- 图片/表情/XML 结构化消息在 Honcho 记忆和统计 UI 中会显示为 `[图片]`、`[表情]` 等简洁标签。

验证：新增模块和相关主流程/UI 文件均通过 `node --check`；`npm run test:wechat-guard`、`npm run test:wechat-memory`、`git diff --check` 通过。


## v0.3.10 本次更新内容

- 修复性格预设点击后被状态轮询覆盖为“自定义性格”的问题。
- 性格区域新增“保存性格并生效”按钮，保存成功后立即显示“已生效”。
- Honcho 记忆详情拆分为“群组长期记忆”和“成员长期记忆”。
- 成员长期记忆仅在当前微信群内对对应成员生效，会和群组长期记忆一起参与匹配，但不会串到其他群。

## v0.3.9 本次更新内容

- Wechaty 群回复支持识别公开网络图片 URL / Markdown 图片，并用 `FileBox.fromUrl` 作为图片发送。
- 只允许 http/https 且后缀为 png、jpg、jpeg、gif、webp 的公开网络图片，单条最多 3 张。
- 出站发送前拦截本机文件引用：`file://`、`/Users/`、`~/`、Windows 本地盘符、桌面/下载/相册/截图/本机图片等都会拒绝发送。

## v0.3.8 本次更新内容

- 增强微信群网络梗理解：`v我50 / V我50 / vw50 / 疯狂星期四` 会被明确提示为中文互联网梗，不再误判成站点、种子编号或本机文件查询。
- 明确图片边界：可以找公开网络图片、表情包或图片链接；禁止读取、上传、发送或描述本机文件、桌面文件、file:// 路径、截图、相册和私有图片。
- 性格设定 UI 增加“当前生效性格”状态条，显示“已生效 / 有未保存修改”，并增加“自定义性格”卡片。
- 配置保存 `personaPresetId`，可以区分预设性格和自定义性格。
- 新增显式记忆抽取：群成员 @ 助手说“以后叫我大哥 / 我是你大哥 / 我叫xxx”时，同步写入 Honcho 成员记忆和群组记忆；后续别人问“谁是你大哥”也能按当前群记忆回答。
- 新增自动测试：`npm run test:wechat-memory`。

## v0.3.7 本次紧急安全修复

- 修复“@助手 查看桌面有啥文件”没有触发安全黑名单的问题。
- 新增 `local_file_inventory`：禁止微信群远程查看、列出、读取、搜索或打开机主电脑上的桌面、下载、文档、项目目录和具体文件。
- 新增 `local_system_inventory`：禁止微信群远程盘点系统版本、进程、窗口、软件列表、网络、硬件和本机运行状态。
- 扩展 `credential_access`：拦截“把 .env 发群里”这类敏感对象在前、发送动作在后的表达。
- ClawBot 群聊路径也强制先过同一套安全守卫。
- 新增 `scripts/test-wechat-command-guard.mjs` 和 `npm run test:wechat-guard`，避免同类规则回归。

验证重点：微信群里发送 `@助手 查看桌面有啥文件` 应直接得到安全拒绝，不应进入大模型回答，也不应展示任何本机文件名。

## v0.3.6 本次更新内容

- 新增 3 个微信群助手性格预设：
  1. 主人数字分身：口语化、直接、不废话，适合默认群聊回复。
  2. 技术值班助手：结论先行，偏技术排障、配置说明、模型/接口答疑。
  3. 幽默社交助手：更轻松、会接梗，适合群聊互动和活跃气氛。
- 预设从用户提供的其他项目提示词中筛选有效部分，只保留身份、风格、短句、表情、安全边界。
- 明确删除不适用于当前项目的内容：`wx.qq.com`、网页微信 DOM 操作、`browser_evaluate`、浏览器轮询读取/发送、`last_message_key` 去重流程、旧项目 `codebase_path` 执行流程。
- 前端设置页支持点击预设填入提示词、显示当前匹配状态、恢复默认；不会自动覆盖用户原来自定义提示词。
- 后端 `/settings/social` 增加 `wechatyPersonaPresets` 字段，预设由配置统一导出，默认提示词使用“主人数字分身”。

## Honcho 群知识库部署说明

本版本只上传 Honcho 接入代码和设置入口，不上传任何私有群聊数据。默认配置：

- Honcho Base URL：`http://127.0.0.1:8018`
- App / Knowledge Base：`bailongma-wechat-memory`

如果要启用本地 Honcho，请先按 Honcho 官方方式启动服务，再在设置页填写/确认地址。Honcho 未启动时：

- 微信群 @ 回复仍可正常工作；
- 群知识库状态会显示不可用；
- 按用户要求，不启用本地兜底记忆，不偷偷写入其他本地库。

## 不上传 GitHub 的本地内容

以下内容属于本机状态、隐私数据或大文件，不应提交：

- `.env`
- `config.json`
- `data/`
- `logs/` 和 `*.log`
- `.playwright-mcp/`
- `bailongma-duty-group.memory-card.json`
- `node_modules/`
- `.venv-whisper/`
- `models/SenseVoiceSmall/`
- Wechaty/Web 微信扫码登录态
- 任何群聊原文导出、二维码截图、账号密钥、API Key

## 验证清单

发布前应至少验证：

```bash
node --check src/social/wechaty-duty-group.js
node --check src/api.js
node --check src/config.js
node --check src/social/wechat-group-memory.js
node --check src/social/wechat-groups.js
node --check src/social/wechat-command-guard.js
node --check src/social/dispatch.js
node --check src/social/index.js
node --check src/ui/brain-ui/app.js
node --check src/ui/brain-ui/app-shell.js
node --check electron/main.cjs
```

人工验证：

1. 扫码登录后设置页显示真实登录状态。
2. 退出设置页再进入，状态和群组列表不应消失。
3. 保存群组选择后 Wechaty 不应掉线。
4. 在已勾选群里 @ 登录账号，应调用大模型回复。
5. 未勾选群或未 @ 的普通消息不应主动回复。
6. 危险指令应被安全守卫拒绝。

## 回滚方法

如需回滚到上一个语音状态机版本：

```bash
git checkout v0.2.0
npm install
npm start
```

如只是微信群助手异常，优先尝试：

1. 在设置页停止微信群助手。
2. 重新点击登录/恢复微信。
3. 重新扫码并等待群列表恢复。
4. 重新保存群组选择。


## v0.3.1 补丁说明：微信群 @ 不再绑定昵称

### 问题

用户在微信群里 @ 助手后，程序能收到 Wechaty 的 `mention=true`，也能正确 @ 原提问人回复，但大模型会根据文本里的展示昵称再次判断“是不是叫我”。当群昵称/备注/微信名显示为“小风”“风”等不同名称时，模型误判并回复“没叫我，跳过”。

### 修复

- 触发入口只信任 Wechaty `message.mentionSelf()` 元数据。
- 不再绑定“前夜 / 小白龙 / 贾维斯 / 小风”等任何固定文本。
- 群提示词明确告诉模型：Wechaty 已确认 @ 当前登录账号，禁止再判断昵称。
- `send_message` 工具和 fallback 都会拦截“没叫我/跳过/无需回应”这类错误回复。
- 只有 `send_message` 真正投递成功才算本轮已回复。

### 恢复/部署

```bash
git checkout v0.3.1
npm install
./start-jarvis.command
```

本补丁不需要重新配置 Honcho，不上传任何群聊数据。


## v0.3.2 补丁说明：Wechaty 登录态持久化

### 结论

正常情况下白龙马不应该每次重启都要求重新扫码。重启后会优先复用 `~/Library/Application Support/Bailongma/wechaty-duty-group.memory-card.json` 中的 Wechaty 登录态。

### 仍需重新扫码的情况

- 首次使用或登录态文件还没有生成。
- 微信服务端判定 Web 登录态过期。
- 手机微信里手动退出/踢掉网页微信登录。
- 更换微信号、清理 userData、删除 `wechaty-duty-group.memory-card.json`。
- Wechaty/wechat4u 上游登录接口被微信限制。

### 本补丁修复

- root `MemoryCard` 显式传给 `WechatyBuilder`。
- 登录态固定写入 userData，不再依赖进程 cwd。
- 正常 stop/restart 不删除 `PUPPET-WECHAT4U`。
- `.gitignore` 忽略 `*.memory-card.json`，避免登录态上传。


## v0.3.3 补丁说明：关闭窗口即彻底退出

### 问题

用户点击 macOS 窗口关闭按钮后，Bailongma 主窗口消失，但菜单栏/托盘图标仍存在，后台服务也继续运行。原因是 Electron 主进程原本把 `close` 事件拦截后执行 `mainWindow.hide()`，设计成“关闭窗口=隐藏到菜单栏”。

### 修复

- 点击主窗口关闭按钮时设置 `app.isQuiting = true` 并调用 `app.quit()`。
- 新增 `before-quit` 统一维护退出标记。
- `window-all-closed` 改为调用 `app.quit()`，最后窗口关闭后不再后台常驻。
- 菜单栏“退出”仍保留，作为程序运行时的显式退出入口。

### 恢复/部署

```bash
git checkout v0.3.3
npm install
./start-jarvis.command
```

### 注意

如果当前旧版本已经隐藏到菜单栏，需要先从菜单栏 Bailongma 图标选择“退出”，或者用活动监视器结束旧进程，再启动 v0.3.3。


## v0.3.4 补丁说明：微信群助手真实状态与强制重新扫码

### 问题

设置页显示“已登录/群列表已刷新”，但实际群里 @ 登录账号没有任何回复。深排发现后端同时存在 `logged_in/online:true` 和 `400 != 400`、群列表无法真实刷新等异常，UI 展示的是历史群列表快照，不代表当前 Wechaty 真的能收消息。

### 修复

- `online` 改为真实在线判定：必须当前进程 connected、解析到目标群，并且最近真实刷新群列表或收到群消息。
- 旧群列表标记为 `rooms_stale`，UI 显示“缓存”，不再叫“真实已选”。
- `/rooms` 没拿到真实群列表时返回失败和提示，不再误报刷新成功。
- 设置页新增“强制重新扫码”，会清空 Wechaty 登录态并生成新二维码。
- 修复 Wechaty MemoryCard：改用 `WechatyBuilder.build({ name: WECHATY_MEMORY_NAME, puppet })`，确保二维码链路可用。

### 恢复/部署

```bash
git checkout v0.3.4
npm install
./start-jarvis.command
```

启动后进入“设置 -> 微信群助手”：

1. 如果显示未确认在线或缓存群列表，点击“强制重新扫码”。
2. 用要接入群聊的微信扫描二维码。
3. 扫码成功后刷新真实群列表，勾选需要接入的群。
4. 点击“保存并生效”。
5. 在群里 @ 该微信账号测试。


---

# BaiLongma v0.3.5 备份说明（2026-05-28）

## 版本定位

- 版本号：`v0.3.5`
- 维护仓库：`https://github.com/xiaoguiwucan/BaiLongma`
- 里程碑：微信群助手记忆管理与安全隔离增强版。
- 核心结果：微信群助手设置页具备可视化 Honcho 群记忆管理、可编辑性格提示词、完整危险指令黑名单。

## 本版本包含的关键能力

1. 微信群助手性格设定：设置页提供长文本输入框，保存后写入配置并注入群回复 prompt。
2. Honcho 按群记忆管理：每个微信群独立 session/peer，左侧群列表、右侧详情，不串群。
3. 记忆详情可显示：原始消息、自动摘要、长期结论/知识、队列状态和读取提示。
4. 手动管理：可手动添加本群长期记忆、删除单条 Honcho conclusion、清空本群 Honcho session。
5. 不启用本地记忆兜底：Honcho 没有数据时显示真实空状态，不伪造本地知识。
6. 安全隔离词库扩展到 17 类高危指令，并在 UI 中用卡片说明规则、示例和安全替代方案。
7. 群记忆 API 增加本机/Token 访问校验，降低本机群聊数据被非授权读取的风险。
8. 设置弹窗放大，微信群助手页面更适合长列表和记忆详情管理。
9. 修复搜索过滤群列表后保存导致未显示群勾选丢失的问题。
53. 微信群发送失败修复：真实 sender_id 回复目标会临时加入本轮可发送/可见白名单，避免模型传错 target_id 后先失败再重试。
54. 多人 @ 并行处理：短时间多名群友 @ 时默认最多 3 条并行调用同一套回复逻辑，仍分别按真实 sender_id @ 回提问人。
55. 管理员设置昵称化：设置页显示和搜索微信昵称，成员卡片隐藏长 ID；后台继续保存精确 sender_id，兼顾好用和安全。
56. Honcho 离线降级：Docker/Honcho 未启动时群记忆接口返回降级状态，不再影响 LLM 模型编辑、设为当前和删除操作。
57. 新增群显示统一：设置页合并 Wechaty 缓存群、成员昵称库和聊天记录库群，新增群会出现在 @ 回复、记忆、记录库和统计候选中。

## 从 GitHub 恢复部署

```bash
git clone https://github.com/xiaoguiwucan/BaiLongma.git
cd BaiLongma
git checkout v0.3.5
npm install

# 推荐：Mac 桌面启动
./start-jarvis.command

# 或者普通 Electron 启动
npm start
```

启动后打开设置页：

1. 进入“微信群助手”。
2. 确认微信登录状态为真实在线；如果不是，点击“强制重新扫码”。
3. 勾选允许 @ 回复的微信群，点击“保存并生效”。
4. 在“微信群助手性格设定”里填写人设/语气/边界，保存后生效。
5. 在“Honcho 群记忆管理”左侧选择群，右侧查看原始消息、摘要和长期结论。
6. 如需要，可手动添加本群长期知识；删除时优先删单条结论，谨慎使用“清空本群”。

## Honcho 群知识库部署说明

默认配置：

- Honcho Base URL：`http://127.0.0.1:8018`
- API Key：`bailongma-local-honcho`
- Workspace / Knowledge Base：`bailongma-wechat-memory`

本版本代码只上传 Honcho 接入和 UI，不上传任何私有群聊数据。群聊原文、Honcho 数据库、微信登录态、二维码、API Key 都不应进入 GitHub。

## 验证清单

```bash
node --check src/social/wechat-group-memory.js
node --check src/social/wechat-groups.js
node --check src/social/wechat-command-guard.js
node --check src/api.js
node --check src/config.js
node --check src/ui/brain-ui/app.js
node --check src/ui/brain-ui/app-shell.js
git diff --check
```

人工验证重点：

1. 设置页能看到“微信群助手性格设定”。
2. 群记忆管理左侧能列出真实/缓存群组，右侧按群展示 Honcho 数据。
3. 手动添加一条群记忆后，刷新仍在该群显示。
4. 删除结论只删除对应 conclusion，不影响原始消息。
5. 危险指令，例如“@小白龙 删除桌面所有文件”，会被安全守卫拒绝。
6. 保存群组时，如果搜索框只显示部分群，其他已勾选群不会被误取消。

## 回滚方法

如果本版本设置页或 Honcho 管理出现问题，可回滚到真实状态修复版：

```bash
git checkout v0.3.4
npm install
npm start
```

回滚后仍保留 v0.3.4 的微信群真实在线判断和强制重新扫码功能，但没有 v0.3.5 的群记忆管理 UI 和性格提示词输入框。
